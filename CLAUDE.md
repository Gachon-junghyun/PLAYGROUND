# PLAYGROUND

여긴 실험장이다. 사용자가 **검증 없이 디스패치로 던지는** 작업이 대부분이니, 결과물이 그대로 굴러갈 수 있어야 한다. 아래 규칙은 디스패치되든 로컬에서 직접 시키든 동일하게 적용된다.

---

## 1. 디렉토리 규칙

모든 신규 작업은 **`PLAY{N}_{snake_case_name}/`** 형태의 독립 디렉토리에 만든다.

- `N`은 순차 번호. 새 PLAY를 시작할 때는 루트에서 `PLAY*` 디렉토리를 훑어보고 가장 큰 N에 +1.
- 이름은 영문 snake_case. 한국어/공백 금지. 예: `PLAY1_csv_dedupe`, `PLAY2_chart_renderer`.
- **PLAY 간 의존 금지.** PLAY는 독립된 실험이다. 다른 PLAY의 코드를 import 하지 마라. 공통 로직이 정말 필요하면 사용자에게 먼저 물어봐라. **유일한 예외는 `core/`** (아래 §9) — 검증된 인프라만 모아둔 곳이라 PLAY가 import해도 된다.
- 기존에 있는 `module_text_chart/`는 레거시라서 명명 규칙에서 제외. 건드리라는 지시가 없으면 그대로 둔다.
- `core/`도 명명 규칙(PLAY{N}_) 제외 — PLAY가 아니라 PLAY들이 딛는 공용 인프라 패키지다. §9 참고.

PLAY 안 내부 구조는 PLAY 성격에 맞게 자유. 단, 루트에 **`README.md`는 필수**.

---

## 2. README.md는 계약서다

검증을 안 하므로, README가 사용자와의 유일한 접점이다.

**언제 쓰고 갱신하나:**
- 새 PLAY를 만들 때 무조건 작성.
- **명령(=지시)이 들어와서 코드/스펙/실행법이 바뀌면, 같은 턴 안에서 README도 갱신.** 코드만 고치고 README를 안 건드리면 작업 미완료로 간주.
- README와 코드가 어긋나면 README가 거짓말이 된다 — 그럴 바엔 README에 "TODO: 이 부분 동기화 필요"라고라도 적어둔다.

**최소 섹션** (`_TEMPLATE_README.md` 참고):
1. **목적** — 한 줄. 이 PLAY가 뭐 하는 놈인지.
2. **실행법** — 복붙해서 돌릴 수 있는 명령. Python 버전, 의존성 설치 방법 포함.
3. **입력 / 출력** — 어떤 인자/파일을 받고 무엇이 나오는지.
4. **가정 & 제약** — 검증 없이 진행한 부분, 미해결 엣지케이스, 모호했던 요구를 어떻게 해석했는지. **이게 가장 중요하다.**
5. **변경 이력** — 날짜 + 한 줄. 디스패치마다 무슨 변경이 있었는지 추적.

---

## 3. 디스패치 모드에서 지켜야 할 것

사용자는 검증을 거의 안 한다. 즉 작업 종료 시점에:

- **실행 가능한 상태**로 끝내라. import 에러, 미완성 함수, 주석 처리된 핵심 로직 같은 걸로 끝내지 말 것. 정 못 끝낼 일이면 README "가정 & 제약"에 명시.
- **모호한 요구는 합리적으로 해석하고 README에 적어둔다.** 사용자에게 되묻는 라운드트립이 비싸다. 단, 데이터 손실 가능성·외부로 나가는 액션·돈 들어가는 일은 예외 — 이런 건 무조건 멈추고 묻는다.
- **모르는 라이브러리·API에 의존하지 마라.** 표준 라이브러리 우선. 외부 의존이 필요하면 README "실행법"에 정확한 설치 명령(`pip install xxx==1.2.3`) 명시.
- **시크릿/자격증명을 코드에 박지 마라.** `.env`, API 키, 토큰 등은 만들지도 말고 commit 가능한 형태로 두지도 마라. 필요하면 README에 환경변수 *이름만* 적고 사용자가 채우게 한다.
- **데이터를 읽어들이는 PLAY라면 샘플 입력**(작은 더미 파일이나 README 안의 인라인 예시)을 같이 둬라. 입력이 없으면 사용자가 검증조차 못 한다.

---

## 4. 실행 시간 제약 (디스패치는 Bash가 ~45초)

디스패치 환경에서 **Bash 한 호출은 약 45초 안에 끝나야 한다.** 넘기면 잘린다. 이걸 어기면 결과물이 깨진 상태로 종료된다.

**그래서:**

- **무거운 의존성 설치 금지.** `pip install torch`, `tensorflow`, `playwright install` 같은 거 절대 코드 실행 경로에 두지 마라. 정 필요하면 README "실행법"에 *사전 설치 명령*으로 분리해서 사용자가 미리 깔게 한다.
- **데이터셋 다운로드, 모델 훈련, 큰 빌드, 거대 파일 압축/해제 금지.** 작업 흐름에 이런 게 끼어 있으면 PLAY 자체가 디스패치에 적합하지 않다는 뜻. README에 "오프라인 사전 준비 필요"라고 명시하고 데이터는 사용자가 미리 깔아둔 경로를 가정한다.
- **검증용 실행은 짧게.** PLAY 동작 확인을 위해 직접 돌려볼 거면 입력을 작게 잡아서 몇 초 안에 끝나도록. 큰 입력으로 검증하다 잘려서 결과를 못 보는 게 최악.
- **꼭 길어져야 한다면 `_REFERENCE/long_running.md`의 패턴을 따라라.** 핵심: PY를 `python -u xxx.py > run.log 2>&1`로 만들고 Bash 툴에 `run_in_background: true`로 띄운다 → sentinel(`DONE`/`FAILED`) 폴링 → Read로 결과 확인. 스크립트 작성 규칙(`flush=True`, 진행 라인, sentinel)도 거기 있다.
- **시간이 걸릴 위험이 있는 부분은 README "가정 & 제약"에 적어둬라.** 예: "이 스크립트는 입력 1만 행까지 ~30초 안에 끝나는 걸 가정. 그 이상은 디스패치에서 잘릴 수 있음."

**한 줄 요약:** 디스패치는 짧고 가벼운 작업에만 적합하다. PLAY 설계 단계에서 이걸 깔고 들어가라.

---

## 5. 코드 스타일 (디스패치라서 더 중요)

- **단순하게.** 추상화·미래 확장성 위한 디자인 금지. 작업이 요구하는 것만 만든다.
- 주석은 **WHY가 비자명할 때만**. WHAT은 코드가 말한다.
- 파일은 가능하면 적게. 한 PLAY가 50줄짜리 한 파일로 끝나면 그게 정답이다.
- 테스트는 사용자가 명시적으로 시키지 않는 한 만들지 마라. 단, "동작 확인"용 짧은 `__main__` 블록이나 README 안의 실행 예시는 환영.
- Windows 환경(PowerShell)에서 돌아간다는 걸 잊지 마라. 경로 구분자, 줄바꿈, `python` vs `python3` 등.

---

## 6. 새 PLAY 시작 절차

1. 루트의 기존 `PLAY*` 중 최대 N을 파악.
2. `PLAY{N+1}_{name}/` 디렉토리 생성.
3. `_TEMPLATE_README.md`를 그 안에 `README.md`로 복사하고 채운다.
4. 코드 작성. 중간에 가정한 게 있으면 README "가정 & 제약"에 그때그때 추가.
5. 끝낼 때 README "변경 이력"에 오늘 날짜 + 한 줄 요약 append.

---

## 7. 기존 PLAY 수정 절차

1. 해당 PLAY의 README를 먼저 읽는다 — 이전 가정/제약을 어겨선 안 된다.
2. 변경 작업 수행.
3. README의 영향받는 섹션(실행법/입력 출력/가정) 갱신.
4. "변경 이력"에 날짜 + 한 줄 추가.

---

## 8. 안 하는 것

- 다른 PLAY 디렉토리 건드리기 (지시 없이).
- 루트에 직접 `.py` 파일 흩뿌리기 — 모든 작업물은 PLAY 디렉토리 안으로.
- README 없이 작업 종료.
- "TODO" 주석만 잔뜩 남기고 끝내기. TODO는 README에 모은다.
- 사용자에게 진행 상황 보고하면서 README는 그대로 두기 — 보고와 README가 어긋나면 README가 진실이 되어야 한다.

---

## 9. core — 공용 인프라 (PLAY 의존 금지의 유일한 예외)

루트의 `core/` 패키지는 실험이 아니라 **실험들이 딛고 서는 바닥**이다. 여러 PLAY가 복붙해 쓰던 인프라를 한 곳으로 승격해둔 곳. 자세한 계약은 `core/README.md`.

**언제 core를 쓰나 (소비):** PLAY가 YouTube 다운로드/전사나 OHLCV 페치가 필요하면 새로 짜지 말고 `core`를 import. 무설치 path shim 2줄(`core/README.md`에 스니펫):
```python
import sys, pathlib
for _p in pathlib.Path(__file__).resolve().parents:
    if (_p / "core" / "__init__.py").exists():
        sys.path.insert(0, str(_p)); break
from core.media import download, run_batch       # YouTube + Whisper
from core.market import fetch, write_csv         # OHLCV
```

**언제 core에 *올리나* (승격) — 게이트, 셋 다 만족해야 함:**
1. **3개+ PLAY가 실제로 쓴다.**
2. **지루하고 안정적** — 자주 안 바뀜.
3. **인터페이스가 깔끔** — `fetch(ticker) -> rows` 수준.
   애매하면 PLAY 안에 인라인으로 두고, 3번째 PLAY가 또 쓰면 그때 승격. **잡동사니 서랍 만들지 마라.** 새로 뭔가 core에 올릴 땐 사용자에게 먼저 확인.

**절대 규칙:**
- **의존 방향은 `PLAY → core` 한 방향.** core는 어떤 PLAY도 import하지 않는다. (core가 PLAY를 알면 죽은 실험이 core를 깨뜨린다.)
- **무거운 의존성은 함수 안에서 lazy import.** `import core`만으로 torch/whisper가 안 끌려와야 함. 설치(`pip install faster-whisper` 등)는 사전 준비 — 45초 Bash 실행 경로에 두지 마라.
- **출력 경로는 인자로.** core 함수는 자기 폴더에 안 쓴다. 소비자가 경로를 넘긴다.

**현재 등재:** `core.media`(YouTube 발견/다운로드/Whisper 전사 — PLAY5/6/7·PLAY33 통합), `core.market`(OHLCV, yfinance→KRX→dummy — PLAY15 승격).

**마이그레이션은 점진적.** 기존 PLAY들은 아직 자기 복사본을 들고 있다. core로 갈아끼우는 건 PLAY별로 따로, 지시받을 때.
