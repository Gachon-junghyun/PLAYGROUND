# PLAY9 QLoRA dependencies — Unsloth-based (RTX 3080 / Windows 11 / Python 3.10-3.12)
#
# Install order:
#   1. PyTorch + torchvision with CUDA (same index, important):
#        pip uninstall -y torch torchvision torchaudio
#        pip install --no-cache-dir "torch==2.8.0" "torchvision==0.23.0" --index-url https://download.pytorch.org/whl/cu128
#      (replace cu128 with your CUDA - check via nvidia-smi)
#   2. Then: pip install -r requirements.txt
#
# Unsloth bundles compatible versions of transformers/peft/trl/bnb that
# actually work together with Gemma 4. Don't override these manually.

unsloth>=2026.5.0
sentencepiece>=0.2.0
safetensors>=0.4.5
huggingface_hub>=0.26.0
